version: '3.9'

services:
  multimodal-api:
    build: .
    container_name: multimodal_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
