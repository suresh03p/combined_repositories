# Enterprise Concurrent Data Collection System

This project demonstrates a concurrent data collection pipeline using asyncio, aiohttp, ThreadPoolExecutor, ProcessPoolExecutor, logging, configuration, and type hints.

## Run

```bash
python app.py
```

The script will collect data from the configured APIs, process it, generate reports, and export them as CSV and JSON.
